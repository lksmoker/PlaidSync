entrypoint = "api.py"  # ✅ Ensure the correct entrypoint is used

modules = ["python-3.11"]

[nix]
channel = "stable-24_05"

[unitTest]
language = "python3"

[gitHubImport]
requiredFiles = [".replit", "replit.nix"]

[deployment]
run = ["sh", "-c", "python api.py"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 80
externalPort = 80

[[ports]]
localPort = 3000
externalPort = 3000

[workflows]
runButton = "Run API"

[[workflows.workflow]]
name = "Run API"
author = 39547853

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "python api.py"
